<fieldset>
  <%= simple_form_for @batch, class: "form-horizontal" do |f| %>
    <%= yield f %>
    <%= f.error_notification %>

    <div class="form-inputs">
      <%= f.association :course, input_html: {class: "form-control"}, autofocus: true, prompt: "Choose a Course" %>
    </div>

    <div class="form-group"></div>
    <div class="form-inputs">
      <%= f.input :batch_name, input_html: {class: "form-control"}, placeholder: "Bactch Name" %>
    </div>
    <div class="form-group"></div>
    <div class="form-inputs">
      <%= f.input :start_date, input_html: {class: "form-control", :data => {'date-format' => 'YYYY/MM/DD'}}, as: :string, :placeholder => "dd-mm-yy" %>
    </div>
    <div class="form-group"></div>
    <div class="form-inputs">
      <%= f.input :end_date, input_html: {class: "form-control", :data => {'date-format' => 'YYYY/MM/DD'}}, as: :string, :placeholder => "dd-mm-yy" %>
    </div>

    <div class="form-group"></div>

    <div class="form-actions">
      <%= f.button :submit, class: "btn btn-primary" %>
    </div>
    
    <div class="form-group"></div>
  <% end %>
</fieldset>

