<p id="notice"><%= notice %></p>

<% @batch.attributes.each do |key, value| %>
  <strong><%= key %></strong> : <%= value %></br>
<% end %>


<%= link_to 'Edit', edit_batch_path(@batch) %> |
<%= link_to 'Back', batches_path %>
